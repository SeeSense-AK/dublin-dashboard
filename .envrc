# --- Conda auto-activation for direnv ---
if [ -f "/opt/anaconda3/etc/profile.d/conda.sh" ]; then
  source /opt/anaconda3/etc/profile.d/conda.sh
  # use "exec" trick so PS1 updates correctly in current shell
  conda activate spinovate >/dev/null 2>&1
  export PS1="(spinovate) $PS1"
fi

